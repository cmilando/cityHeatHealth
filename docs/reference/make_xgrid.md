# An internal function to make the xgrid

An internal function to make the xgrid

## Usage

``` r
make_xgrid(data, column_mapping, months_subset = 1:12, dt_by = "day")
```

## Arguments

- dt_by:

  either by day or by week
